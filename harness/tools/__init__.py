"""harness.tools — the tool surface: what she has, and what it can do.

See manifest.py. The protocol side (MCP servers, the bridge) stays in
harness/mcp_server/; this package is about the CATALOGUE, not the transport.
"""
